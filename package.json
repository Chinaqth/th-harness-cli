{
  "name": "@chinaqth/harness-cli",
  "version": "0.2.1",
  "description": "User-level Harness Engineering gateway for Codex projects.",
  "type": "module",
  "bin": {
    "harness": "bin/harness.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "files": ["bin", "src", "bundle", "README.md", "README-CH.md", "docs"],
  "scripts": {
    "bundle:build": "node scripts/build-bundle.js",
    "bundle:verify": "node scripts/verify-bundle.js",
    "test": "node --test",
    "check": "node --check bin/harness.js && npm run bundle:verify && node --test && npm_config_cache=/tmp/harness-cli-npm-cache npm pack --dry-run"
  }
}
